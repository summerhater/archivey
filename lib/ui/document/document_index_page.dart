import 'package:flutter/material.dart';

class DocumentIndexPage extends StatelessWidget {
  const DocumentIndexPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
