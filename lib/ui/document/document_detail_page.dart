import 'package:flutter/material.dart';

class DocumentDetailPage extends StatelessWidget {
  final String? docId;

  const DocumentDetailPage({super.key, this.docId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
